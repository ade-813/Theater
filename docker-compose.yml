services:
  server:
    build:
      context: ./server
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development

  client:
    build:
      context: ./client
    ports:
      - "5173:5173"
    depends_on:
      - server
