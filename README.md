# Theater

## Running the app

```bash
cd server
npm install
node index.mjs --env-file=.env
```

```bash
cd client
npm install
npm run dev
```

Server runs on http://localhost:3001, client on http://localhost:5173.
